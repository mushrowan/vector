Replace heim with direct `/proc/diskstats` parsing (linux) and sysinfo (other platforms) for the disk collector in the `host_metrics` source, fixing a crash on Windows RAID configurations

authors: mushrowan
